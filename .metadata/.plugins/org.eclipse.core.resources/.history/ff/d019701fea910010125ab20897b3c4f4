package com.announcify.engine.channel;

import java.io.IOException;
import java.util.Random;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import com.announcify.engine.servlet.AuthenticatedServlet;
import com.announcify.engine.servlet.Servlet;
import com.google.appengine.api.channel.ChannelService;
import com.google.appengine.api.channel.ChannelServiceFactory;
import com.google.appengine.api.users.User;

@SuppressWarnings("serial")
public class ChannelServlet extends Servlet {

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
        ChannelService service = ChannelServiceFactory.getChannelService();

        String token = ";" + new Random().nextFloat() + System.currentTimeMillis() + ";";

        service.createChannel(token);

        response.setContentType("text/plain");
        response.getWriter().write(token);
    }
}
